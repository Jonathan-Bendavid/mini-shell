# mini-shell
Mini-Shell is a Windows-based educational command-line shell written in C. It supports built-in commands, execution of external programs, and basic I/O redirection, providing a simplified model of how operating system shells work.
